# Adapt Print static preview

Temporary public GitHub Pages preview for mobile/client review.

- Marked `noindex,nofollow` in every HTML page.
- `robots.txt` blocks crawling.
- Not the production website.
- Production launch still requires WordPress backup, redirect map, final DTF facts, legal text, and form-backend decision.
